<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eventi.left.design.mapper.DesignMapper">
	<select id="designList" resultType="com.eventi.left.design.service.DesignVO">
		SELECT * FROM (SELECT ROWNUM RN, A.* FROM (
			SELECT *
			FROM design
	  	<![CDATA[
			) A WHERE ROWNUM <= #{last}) WHERE RN >= #{first}
		]]>
	</select>
	
	<select id="count" resultType="int">
		SELECT COUNT(*)
		FROM DESIGN
	</select>
	
	<select id="contestDesignList" resultType="DesignVO">
		select c.c_no, d.dgn_no, d.user_id d_user_id, d.name, d.cntn, d.dt, d.center_img, d.pubc_yn
		from contest c join design d
		                on c.c_no = d.c_no    
		and c.c_no = #{cNo}
	</select>
	
</mapper>