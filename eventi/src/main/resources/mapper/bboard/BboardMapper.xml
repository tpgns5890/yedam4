<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eventi.left.bboard.mapper.BboardMapper">
	
	<!-- type별 전체 게시글 조회 -->
	<select id="bboardList" resultType="com.eventi.left.bboard.service.BboardVO">
		SELECT *
		FROM B_BOARD
		WHERE TYPE = #{type}
	</select>
	
	<!-- 게시글 단건 조회 -->
	<select id="bboardSelect" resultType="com.eventi.left.bboard.service.BboardVO">
		SELECT *
		FROM B_BOARD
		WHERE B_BOARD_NO = #{bBoardNo}
	</select>
	
	<!-- 게시글 등록 -->
	<insert id="bboardInsert">
		INSERT INTO B_BOARD(B_BOARD_NO, WRITER, TTL, LCLSF, MCLSF, CNTN, IMG, TYPE)
		VALUES('BB'||BBOARD_SEQ.NEXTVAL, #{writer}, #{ttl}, #{lclsf}, #{mclsf}, #{cntn}, #{img}, #{type}) 
	</insert>
	
	<!-- 게시글 수정 -->
	<update id="bboardUpdate">
		UPDATE B_BOARD
		SET TTL=#{ttl}, CNTN=#{cntn}, IMG=#{img}
		WHERE B_BOARD_NO = #{bBoardNo}
	</update>
	
	<!-- 게시글 삭제 -->
	<delete id="bboardDelete">
		DELETE FROM B_BOARD
		WHERE B_BOARD_NO = #{bBoardNo}
	</delete>
</mapper>